name: email trigger

on:
  schedule:
    - cron: '07 00 * * *'
      timezone: "Asia/Kolkata"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  Email_Trigger:
    name: Email_Trigger
    runs-on: ubuntu-22.04

    steps:
      - name: Code Checkout
        uses: actions/checkout@v4

      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install requests beautifulsoup4

      - name: Python Check
        run: |
          which python
          python --version
          python3 --version

      - name: Check Files
        run: |
          pwd
          ls -la

      - name: Trigger Emails
        run: |
          python process_emails2.py
